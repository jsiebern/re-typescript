mapped object types › treats the optional status correctly
type a = {
  x: string,
  y: option(float),
  z: bool,
}
and same_P =
  | X
  | Y
  | Z
and same = {
  x: string,
  y: option(float),
  z: bool,
}
and req_P =
  | X
  | Y
  | Z
and req = {
  x: string,
  y: float,
  z: bool,
}
and part_P =
  | X
  | Y
  | Z
and part = {
  x: option(string),
  y: option(float),
  z: option(bool),
};

