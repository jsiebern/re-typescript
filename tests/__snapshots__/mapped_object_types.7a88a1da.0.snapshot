mapped object types › can use mapper ident type parameters
module Test = {
  type proxy('T) = {
    get: unit => 'T,
    set: 'T => unit,
  };
  type a = {
    x: string,
    y: float,
    z: bool,
  };
  type proxied = {
    x: proxy(string),
    y: proxy(float),
    z: proxy(bool),
  };
};

