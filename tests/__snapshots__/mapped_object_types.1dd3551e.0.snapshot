mapped object types › resolves nested type args when using mapped obj types
module Test = {
  type a('X) = {
    x: string,
    y: 'X,
    z: bool,
  };
  type keys =
    | X
    | Y;
  type stripped_T_X = {obj: float};
  type stripped = {
    x: string,
    y: stripped_T_X,
  };
};

