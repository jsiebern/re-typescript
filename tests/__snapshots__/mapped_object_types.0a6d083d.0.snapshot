mapped object types › can use type param (no extraction) as map target
module Test = {
  type record_K =
    | A
    | B;
  type record_T = {field: bool};
  type record = {
    a: record_T,
    b: record_T,
  };
};

